<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giriş Yap</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body class="auth-body">

    <div class="auth-container">
        <div class="auth-header">
            <h2>Oturum Aç</h2>
            <p>Arıza Takip Sistemi</p>
        </div>

        <% if (error) { %>
            <div class="alert-error">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="#d93025"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/></svg>
                <span><%= error %></span>
            </div>
        <% } %>

        <form action="/login" method="POST">
            
            <div class="material-group">
                <input type="text" id="sicil" name="sicil" required placeholder=" " autofocus>
                <label for="sicil">Sicil Numarası</label>
            </div>

            <div class="material-group">
                <input type="password" id="sifre" name="sifre" required placeholder=" ">
                <label for="sifre">Şifre</label>
            </div>

            <div style="text-align:left; margin-bottom:20px;">
                <a href="#" style="color:#1a73e8; font-size:14px; text-decoration:none; font-weight:500;">Şifremi unuttum?</a>
            </div>

            <button type="submit" class="btn-google">İleri</button>
        </form>

        <div class="auth-footer">
            Hesap oluşturun <a href="/register">Kayıt Ol</a>
        </div>
    </div>

</body>
</html>